FORMAT: 1A
HOST: http://localhost:1317

# THORChain API
This documentation outlines the API for the [BEPSwap](bepswap.com) THORChain API.

NOTE: This document is a **work in progress**.

# Group Health Check

## Ping [/thorchain/ping]

### Ping [GET]

+ Response 200 (application/json)
    {
        "ping": "pong"
    }


# Group Pools
This section documents the pools API resources.

## Get a Pool [/thorchain/pool/{ticker}]

+ Parameters
    + ticker: "bnb" (required, string) - The ticker of the pool

### Get a Pool [GET]
+ Response 200 (application/json)
    + Attributes (Pool)

## Get Pool List [/thorchain/pools]

### Get Pools [GET]
+ Response 200 (application/json)
    + Attributes (array[Pool])

## Get List of liquidity providers for a pool [/thorchain/pool/{ticker}/liquidity_providers]

+ Parameters
    + ticker: "bnb" (required, string) - The ticker of the pool

### Get Pools [GET]
+ Response 200 (application/json)
    + Attributes (LiquidityProvider)


# Group Tx

## Get a Tx [/thorchain/tx/{hash}]

+ Parameters
    + ticker: "E17A0906E015F0C343691C18E475C8CB5F3F6C63F5BCDE0F3A341909763CC92B" (required, string) - Tx hash of a binance transaction

### Get TX Information [GET]
+ Response 200 (application/json)
    + Attributes (Tx)



## Data Structures

### Pool
+ balance_rune: Amount of rune the pool has
+ balance_asset: Amount of asset the pool has
+ ticker: the ticker of the non-rune asset
+ pool_units: total number of units within the pool
+ status: the status of the pool

### LiquidityProvider
+ asset: the asset of the non-rune asset
+ units: the total number of units of the pool
+ rune_address: the rune address of the liquidity provider
+ asset_address: the non-rune asset address of the liquidity provider
+ last_add block height of the last add
+ last_withdraw: block height of the last withdraw
+ pending_rune: rune that has been staked, but not committed yet. Awaiting a staking of the non-rune asset

### Tx
+ request: request tx hash
+ status: status of tx
+ done: completed tx hash
+ sender: bnb address of requester
